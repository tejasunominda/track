/**
 * Contextual left sidebar (collapsible): global-level (Your Work, Projects,
 * Filters, Dashboards) or project-level (Board, Backlog, Reports, ...) nav.
 * See Frontend Specification Document §3.2.
 */
export function SidebarNav() {
  return <nav className="w-56 shrink-0 border-r border-gray-200 bg-gray-50 p-2" />;
}
